# zig-crypto

> Portable cryptographic primitives in Zig with a stable C FFI -- SHA-256, HMAC-SHA-256, AES-CBC, PBKDF2, ECDH P-256, Ed25519, and CSPRNG. No OpenSSL. No CommonCrypto. No system crypto dependencies.

Part of the Tinyland Zig Libraries.

zig-crypto is the pure-Zig crypto proof for a family of small native capability layers. The pattern is a hermetic Zig implementation, a stable C ABI for app integration, and a Zig package root for direct Zig consumers.

Positioning: the C ABI is the stable boundary between app developer experience and native crypto implementation. In Tinyland planning this supports de-attestation: SwiftUI, UIKit, AppKit, Cocoa, or Objective-C apps keep their presentation and lifecycle code while Apple-shaped crypto calls move behind portable Zig contracts.

Available Apple analogs: CryptoKit SHA-256, HMAC-SHA-256, P-256 ECDH primitive shape, and `Curve25519.Signing` Ed25519-style signing shape.

CommonCrypto-style SHA/HMAC, AES-CBC, and PBKDF2-SHA1 are separate migration analogs. Security.framework-style random bytes map to `zig_crypto_random`. Swift/ObjC parity gaps remain for SwiftPM/modulemap packaging, Swift `Data` wrappers, Objective-C samples/nullability, `NSError` bridging, CommonCrypto-compatible aliases, CryptoKit `SharedSecret`/HKDF/key-type parity, CryptoKit AES-GCM/key-wrap parity, and streaming C contexts.

## Installation

```
zig fetch --save git+https://github.com/Jesssullivan/zig-crypto.git
```

## Full Documentation

- [Documentation](https://transscendsurvival.org/zig-crypto/): Published docs site
- [Documentation source](https://github.com/Jesssullivan/zig-crypto/tree/main/docs): Markdown source
- [C FFI reference](https://transscendsurvival.org/zig-crypto/api/c-ffi/): C ABI docs
- [Zig API reference](https://transscendsurvival.org/zig-crypto/api/zig-api/): Zig module docs
- [Apple interop guide](https://transscendsurvival.org/zig-crypto/guides/apple-interop/): Swift/ObjC analogs, parity gaps, and good first issues
- [llms-full.txt](https://raw.githubusercontent.com/Jesssullivan/zig-crypto/main/llms-full.txt): Complete API dump for LLMs
- [AGENTS.md](https://github.com/Jesssullivan/zig-crypto/blob/main/AGENTS.md): Agent instructions

## C API (17 functions)

- `zig_crypto_sha256(data, len, out)`: SHA-256 hash (out: 32 bytes)
- `zig_crypto_sha256_hex(data, len, out)`: SHA-256 hex string (out: 64 bytes)
- `zig_crypto_hmac_sha256(key, key_len, data, len, out)`: HMAC-SHA-256 (out: 32 bytes)
- `zig_crypto_aes128_cbc_encrypt(key, iv, pt, pt_len, out)`: AES-128-CBC encrypt, PKCS#7
- `zig_crypto_aes128_cbc_decrypt(key, iv, ct, ct_len, out)`: AES-128-CBC decrypt, PKCS#7
- `zig_crypto_aes256_cbc_encrypt(key, iv, pt, pt_len, out)`: AES-256-CBC encrypt, PKCS#7
- `zig_crypto_aes256_cbc_decrypt(key, iv, ct, ct_len, out)`: AES-256-CBC decrypt, PKCS#7
- `zig_crypto_aes256_cbc_encrypt_raw(key, iv, pt, pt_len, out)`: AES-256-CBC raw (CTAP2)
- `zig_crypto_aes256_cbc_decrypt_raw(key, iv, ct, ct_len, out)`: AES-256-CBC raw (CTAP2)
- `zig_crypto_pbkdf2_sha1(pass, pass_len, salt, salt_len, iters, out, out_len)`: PBKDF2-SHA1
- `zig_crypto_p256_generate(scalar, pub_x, pub_y)`: P-256 keygen (0/-1)
- `zig_crypto_p256_ecdh(scalar, peer_x, peer_y, shared)`: ECDH shared secret (0/-1)
- `zig_crypto_ed25519_generate(seed, pub, signing)`: Ed25519 keygen
- `zig_crypto_ed25519_from_seed(seed, pub, signing)`: Ed25519 from seed (0/-1)
- `zig_crypto_ed25519_sign(msg, len, signing, sig)`: Ed25519 sign (0/-1)
- `zig_crypto_ed25519_verify(msg, len, sig, pub)`: Ed25519 verify (bool)
- `zig_crypto_random(buf, len)`: Secure random bytes (bool)

## Build

```
zig build                    # build static library
zig build test               # run unit tests
zig build test-pbt           # property-based tests
zig build docs               # generate API docs
zig build example            # build and run C example
```

## Platform Support

- Cross-platform (Zig standard library only, no system crypto dependencies)
